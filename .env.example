# Multiplayer Configuration
# Set to 'true' to use Colyseus server-based multiplayer
# Set to 'false' (or leave unset) to use WebRTC peer-to-peer multiplayer
VITE_USE_COLYSEUS=false

# Colyseus Server URL (only needed if VITE_USE_COLYSEUS=true)
# For local development: ws://localhost:2567
# For production: wss://your-server.com
VITE_COLYSEUS_SERVER=ws://localhost:2567
